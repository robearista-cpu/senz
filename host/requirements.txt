# Core (all viz)
numpy
pyserial

# GPU 3D viz (live_hand_qt.py) - recommended
pyqtgraph
PyOpenGL
PyQt5

# v2 hand skeleton viz (senz_visualizer.py) - reuses senz_io numpy math (no scipy)
vpython
# vpython 7.6.5 imports pkg_resources; setuptools>=81 removed it (Python 3.12+)
setuptools<81

# Bluetooth (--ble)
bleak

# Legacy CPU viz (live_hand_viz.py) - optional fallback
matplotlib

# Multi-IMU branch (multi-imu-finger-tracking)
# Dataset prep / fusion (dataset_prep.py, fusion/) - needs pandas (numpy already above)
pandas
# Camera tracking (camera_tracker.py, dataset_recorder.py --camera) - optional, heavy
mediapipe
opencv-python
# Friendly camera device NAMES in the scan (Windows DirectShow) - optional; without
# it the scan still works but shows "Camera 0/1/..." instead of the device name.
pygrabber
